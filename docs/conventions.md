# Conventions

Record reusable, non-obvious conventions discovered while implementing Blueprint tasks. Keep this file short; delete or rewrite stale notes.

## Notes

- Add a dated bullet only when a convention will help the next spec, plan, implementation, or review.

## Template

```markdown
- YYYY-MM-DD: <short convention>. Context: <where it applies>. Check: <how to verify it>.
```
