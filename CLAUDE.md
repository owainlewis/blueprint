@AGENTS.md

## Claude Code

Install Blueprint with `npx skills add owainlewis/blueprint`.

Invoke `spec`, `tickets`, `build`, `review`, or `browser-test` by name. Each skill is self-contained. `browser-test` requires an available real-browser automation tool.

`AGENTS.md` only governs contributions to this repository.
