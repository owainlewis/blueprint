@AGENTS.md

## Claude Code

Blueprint is distributed as a Claude Code plugin. The active workflows live in `skills/<name>/SKILL.md`.

- Invoke plugin skills as `/blueprint:design-doc`, `/blueprint:spec`, `/blueprint:plan`, `/blueprint:implement`, `/blueprint:tdd`, `/blueprint:refactor`, `/blueprint:review`, `/blueprint:address-pr-feedback`, `/blueprint:browser-verify`, `/blueprint:explain-visually`, `/blueprint:compress`, `/blueprint:branch`, or `/blueprint:commit`.
- `browser-verify` requires Chrome DevTools MCP.
- Keep shared repo guidance in `AGENTS.md`; keep Claude-specific adapter notes here.
